# Evaluation fixtures

This directory contains synthetic, evaluation-only fixtures. Runtime and agent
code must not consume them.

Expected candidate group keys are stable evaluation labels, not runtime
Candidate IDs, database IDs, or TechnicalDebt IDs. Later work packages may
extend these fixtures for additional evaluation needs.
